import {Text, StyleSheet} from 'react-native';

const Manal =() => {
    return <Text style={Style.text}>Manal</Text>
}

const Style = StyleSheet.create({
   text: {
    fontSize: 32,
    color: 'green',
    textAlign: 'center'
   }
    
})

export default Manal;